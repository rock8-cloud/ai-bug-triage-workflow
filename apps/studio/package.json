{
  "name": "@bugtriage/studio",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --env-file=../../.env --hot serve.ts",
    "build": "bun run build.ts",
    "start": "bun --env-file=../../.env serve.ts",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "mastra": "1.28.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^26.2.0",
    "typescript": "^7.0.2"
  }
}
